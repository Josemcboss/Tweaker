<Window x:Class="Tweaker.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Guardar perfil"
        Height="220"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre del perfil" Foreground="#E0E0E0" Margin="0,0,0,6"/>
        <TextBox x:Name="ProfileNameTextBox" Grid.Row="1" Height="28" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Descripción (opcional)" Foreground="#E0E0E0" Margin="0,0,0,6"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="3" Height="28" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="SaveButton" Content="Guardar" Width="90" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
