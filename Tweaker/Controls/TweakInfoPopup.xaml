<UserControl x:Class="Tweaker.Controls.TweakInfoPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="350" 
             d:DesignWidth="400">
    
    <!-- Popup principal -->
    <Border Background="#1A1A1A" 
            BorderBrush="#5865F2" 
            BorderThickness="2" 
            CornerRadius="8"
            MaxWidth="450"
            MinWidth="350">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            BlurRadius="15" 
                            ShadowDepth="5" 
                            Opacity="0.8"/>
        </Border.Effect>
        
        <StackPanel Margin="20">
            <!-- Header con titulo y boton cerrar -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Titulo con icono de recomendacion -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="RecommendationIcon"
                             Text="{Binding Recommended, RelativeSource={RelativeSource AncestorType=UserControl}, 
                                           Converter={StaticResource BoolToRecommendationIconConverter}}"
                             FontFamily="Segoe UI Emoji" 
                             FontSize="16" 
                             VerticalAlignment="Center"
                             Margin="0,0,8,0"/>
                    <TextBlock x:Name="TitleText" 
                             Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             FontSize="16" 
                             FontWeight="Bold" 
                             Foreground="White"
                             TextWrapping="Wrap"
                             VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Boton cerrar -->
                <Button Grid.Column="1" 
                        Content="X" 
                        Width="25" 
                        Height="25"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#CCCCCC"
                        FontSize="14"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="CloseButton_Click"
                        ToolTip="Cerrar">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF4444"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- Descripcion principal -->
            <TextBlock x:Name="DescriptionText"
                     Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     FontSize="13" 
                     Foreground="#CCCCCC"
                     TextWrapping="Wrap"
                     LineHeight="18"
                     Margin="0,0,0,15"/>
            
            <!-- Beneficios -->
            <Border Background="#0A2A0A" 
                    BorderBrush="#0E7A0D" 
                    BorderThickness="1"
                    CornerRadius="6" 
                    Padding="12"
                    Margin="0,0,0,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="+" 
                                 FontFamily="Segoe UI" 
                                 FontSize="14"
                                 Margin="0,0,8,0"/>
                        <TextBlock Text="BENEFICIOS" 
                                 FontSize="12" 
                                 FontWeight="Bold"
                                 Foreground="#0E7A0D"/>
                    </StackPanel>
                    <TextBlock x:Name="BenefitsText"
                             Text="{Binding Benefits, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             FontSize="11" 
                             Foreground="#CCCCCC"
                             TextWrapping="Wrap"
                             LineHeight="16"/>
                </StackPanel>
            </Border>
            
            <!-- Advertencias (solo si existen) -->
            <Border x:Name="WarningsSection"
                    Background="#2A0A0A" 
                    BorderBrush="#FFC107" 
                    BorderThickness="1"
                    CornerRadius="6" 
                    Padding="12"
                    Margin="0,0,0,10">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Warnings, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Warnings, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="!" 
                                 FontFamily="Segoe UI" 
                                 FontSize="14"
                                 Margin="0,0,8,0"/>
                        <TextBlock Text="ADVERTENCIAS" 
                                 FontSize="12" 
                                 FontWeight="Bold"
                                 Foreground="#FFC107"/>
                    </StackPanel>
                    <TextBlock x:Name="WarningsText"
                             Text="{Binding Warnings, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             FontSize="11" 
                             Foreground="#CCCCCC"
                             TextWrapping="Wrap"
                             LineHeight="16"/>
                </StackPanel>
            </Border>
            
            <!-- Recomendacion final -->
            <Border Background="#1A1D21" 
                    BorderBrush="#5865F2" 
                    BorderThickness="1"
                    CornerRadius="6" 
                    Padding="12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="FinalRecommendationIcon"
                             Text="{Binding Recommended, RelativeSource={RelativeSource AncestorType=UserControl}, 
                                           Converter={StaticResource BoolToRecommendationIconConverter}}"
                             FontFamily="Segoe UI Emoji" 
                             FontSize="14"
                             Margin="0,0,8,0"
                             VerticalAlignment="Center"/>
                    <TextBlock x:Name="FinalRecommendationText"
                             FontSize="11" 
                             FontWeight="SemiBold"
                             Foreground="#5865F2"
                             VerticalAlignment="Center"
                             TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Recommended, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Text" Value="RECOMENDADO: Este tweak es generalmente seguro y beneficioso."/>
                                        <Setter Property="Foreground" Value="#0E7A0D"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Recommended, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                        <Setter Property="Text" Value="AVANZADO: Solo para usuarios experimentados. Revisar advertencias."/>
                                        <Setter Property="Foreground" Value="#FFC107"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>